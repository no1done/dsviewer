extends layout.pug

block content
    h3 Select Kind
    ul
        for kind in kinds
            li
                a(href=`/filter/${namespace || 'default'}/${kind}`) #{namespace || 'Default'} / #{kind}
